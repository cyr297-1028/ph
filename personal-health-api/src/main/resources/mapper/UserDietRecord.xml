<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.kmbeast.mapper.UserDietRecordMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_diet_record (user_id, food_name, calories, protein, fat, carbs, create_time)
        VALUES (#{userId}, #{foodName}, #{calories}, #{protein}, #{fat}, #{carbs}, #{createTime})
    </insert>
</mapper>